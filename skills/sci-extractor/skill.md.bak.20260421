---
name: sci-extractor
description: 从本地SCI文献PDF中提取目标变异的结构化信息。使用Python脚本（PyMuPDF）读取PDF全文和表格，自动提取并生成标准化中文总结段落。
---

# SCI 本地文献变异提取器

从本地 SCI 文献 PDF 文件中提取目标变异的结构化信息，并生成符合 ACMG/临床遗传学报告规范的标准化总结段落。

## 使用方法

```
/sci-extractor
```

默认读取 `D:\claude_code\project1\sci` 目录下所有 PDF 文件。

## 工作流程

1. 扫描 `D:\claude_code\project1\sci` 目录下所有 `.pdf` 文件
2. 运行预置的 Python 脚本（`scripts/sci_extractor.py`）
3. 脚本使用 PyMuPDF (fitz) 提取每篇 PDF 的全文文本和所有表格（失败时自动回退到 pypdf → pdfplumber）
4. 在全文和表格中搜索目标变异相关内容
5. 提取结构化信息（患者详情、共存变异、致病性评级等）
6. 根据遗传模式和**文献实际内容**生成标准化中文总结段落（仅输出文献中确实存在的信息，不杜撰）
7. 结果输出到控制台和 `D:\claude_code\project1\sci\sci_variant_results.json`

## 执行指令

**脚本路径固定，每次直接运行即可：**

```bash
python .claude/skills/sci-extractor/scripts/sci_extractor.py
```

脚本特性：
- 使用 PyMuPDF (fitz) 作为主要提取引擎，支持文本和表格提取
- 自动回退机制：PyMuPDF → pypdf → pdfplumber
- Windows UTF-8 编码兼容
- 自动扫描目录下所有 PDF 文件
- 解析 PDF 中的所有表格，提取完整的表格行数据
- 从正文中提取患者描述和变异上下文
- 自动提取共存变异（co-variant）和反式/顺式位置证据
- 自动提取参考文献信息（作者、期刊、年份、DOI）
- 根据遗传模式（隐性 vs 显性）和实际文献数据生成标准化中文总结段落
- **不杜撰信息**：总结段落中每句话都基于文献中实际存在的数据，如果某条信息在文献中未找到则跳过该句
- 结果以中文输出到控制台和 JSON 文件

## 修改目标变异

**必须修改脚本开头的配置部分，然后运行脚本：**

```python
# 脚本路径: .claude/skills/sci-extractor/scripts/sci_extractor.py
# 第 25-32 行
GENE = "目标基因"
CDNA = "c.NNNX>Y"
PROTEIN = "p.XXX"
PROTEIN_SHORT = "p.XX"
EXTRA_KEYWORDS = ["额外关键词1", "关键词2", ...]
```

修改完成后直接运行 `python .claude/skills/sci-extractor/scripts/sci_extractor.py`，脚本会重新扫描所有 PDF 并提取新变异信息。

## 输出字段

| 字段 | 说明 |
|------|------|
| PMID | PubMed ID（如 PDF 中包含） |
| 一句话概括 | 聚焦目标变异的文献总结 |
| 目标变异是否提及 | 文献是否提及该变异（true/false） |
| 基因 | 目标基因名称 |
| 变异类型 | missense/nonsense/splice/frameshift 等（仅针对目标变异） |
| cDNA 改变 | 如 c.2971A>G |
| 蛋白质改变 | 如 p.Thr991Ala |
| 致病性 | 仅针对目标变异的致病性评级 |
| 遗传方式 | 该变异在患者中是杂合/纯合/复合杂合 |
| 患者详情 | **仅携带目标变异**的患者个体信息（编号、年龄、性别、表型、基因型、籍贯、出生体重等） |
| 临床表型 | 目标变异相关的疾病/症状 |
| 遗传模式 | 常显/常隐等 |
| 功能验证 | 是否对**该变异**进行了功能实验（体外表达、酶活性、结构建模、SIFT/PolyPhen预测等） |
| 功能验证详情 | 描述该变异功能验证的原文句子 |
| 原文相关句 | 文献中直接提及该变异的所有句子 |
| 患者表型 | **仅限与目标变异同行/同表**的患者表型，不包含全文其他患者的表型 |
| 患者额外信息 | **仅限与目标变异同行/同表**的实验室检查值（如 Cp、CuU、LCuEx 等） |
| 共存变异 | 与目标变异在同一患者记录中出现的另一个等位基因变异（仅同行/同表提取） |
| 反式位置确认 | 是否有明确证据（trans/inherited from mother/father）确认反式位置 |
| 患者数量 | 携带目标变异的患者总数 |
| 参考文献信息 | 自动提取的作者、期刊、年份、DOI |
| **总结段落** | **标准化中文描述段落（核心输出，仅包含文献实际信息）** |

## 总结段落生成原则

脚本根据遗传模式和**文献实际存在的数据**生成段落，遵循以下原则：

- **仅输出文献中确实存在的信息**，不杜撰任何内容
- **患者表型仅限目标变异同行/同表**的内容，不包含全文其他患者的表型
- 如果文献中没有提到反式位置证据，则不生成相关句子
- 如果文献中没有功能验证数据，则不添加"尚未经过功能学验证"等表述
- 如果文献中没有共分离数据，则不生成相关句子
- 共存变异仅在目标变异同一行/同一段落中出现时才被提取
- 如果文献中有患者的额外实验室检查值（如 Cp、CuU 等），会添加"实验室检查示..."句子

### 隐性遗传（复合杂合/纯合）

> 1例XX患者为该c.XXX变异与一个致病性或可能致病性c.XXX变异的复合杂合
> 该先证者通过父母检测确认处于反式位置（**仅当文献有trans/父母来源证据时添加**）
> 该患者表型为xx、xx、xx等（**仅限文献中与目标变异同行/同表的患者表型**）
> 实验室检查示xx（**仅当有患者额外实验室检查值时添加**）
> 该变异已经过[实验名称]显示可能影响蛋白功能（**仅当有功能验证时添加**）
> 参考文献：xxx

### 显性/半合子遗传

> 该变异已在X例先证者中报道
> 该患者表型为xx、xx、xx（**仅限文献中与目标变异同行/同表的患者表型**）
> 实验室检查示xx（**仅当有患者额外实验室检查值时添加**）
> 该变异已经过[实验名称]显示可能影响蛋白功能（**仅当有功能验证时添加**）
> 参考文献：xxx

## 注意事项

- 如果 PDF 使用嵌入字体（无 Unicode 映射）或为扫描版，所有文本提取工具均无法读取文字，会提示"无法提取可读文本"
- 对于此类 PDF，建议：1) 使用 OCR 工具（如 Adobe Acrobat OCR）处理后再提取 2) 将 PDF 转换为 Word 格式
- 如文献未提及目标变异，会明确标注"未提及"
- **患者表型仅限与目标变异同行或同表的内容**，不会从全文其他位置提取无关患者的表型
- 患者详情和实验室检查值同样受此限制
- **总结段落**字段为脚本根据原文数据自动生成的标准化描述，不添加原文未提及的信息
